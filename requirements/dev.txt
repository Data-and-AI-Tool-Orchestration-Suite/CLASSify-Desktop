# Development dependencies — NOT included in PyInstaller bundle.
# Used for lint, typecheck, testing, and dev tooling.
#
# To regenerate: pip-compile pyproject.toml -o requirements/dev.txt --extra dev --extra dev-ml

-r runtime.txt

pytest>=8.2
pytest-asyncio>=0.23
pytest-cov>=5
httpx>=0.27
ruff>=0.6
mypy>=1.10
pip-audit>=2.7
pip-tools>=7.4
pre-commit>=3.7
